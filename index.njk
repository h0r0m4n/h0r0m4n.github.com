---
layout: default
---
<header id="about" class="section">
    <div class="t-container">
        <h1>
            <span class="h3">{{ site.author }}</span>
            <br>
            {% include 'logo.svg' %}
        </h1>
        {% include 'avatar.njk' %}
        <p>Nice to meet you <span class="emoji" aria-hidden="true">👋</span>. I’m a Ukrainian multidisciplinary designer based in {{ site.location }}.</p>
        <p>My work focuses on building thoughtful, intuitive, and delightful digital products with a devotion to the quality, design process and transparency.</p>
        {% if skills %}
            <ul class="t-list-horizontal">
                {% for expertise in skills %}
                    <li class="expertise t-list-horizontal--item t-list-horizontal--item--distance-2">
                        {% include expertise | slug + '.svg' %}
                        <small>{{ expertise }}</small>
                    </li>
                {% endfor %}
            </ul>
        {% endif %}
    </div>
</header>

<section id="works" class="section">
    <div class="t-container">
        <h2>Case studies</h2>
        <p>My portfolio includes 50+ projects, here’s the selected one:</p>
    </div>
    <ol>
        {% for work in collections.work %}
            {% if work.url != false %}
                <li class="work work--reels">
                    <a class="work-link t-hover--2" href="{{ work.url | url }}" rel="bookmark" target="_parent">
                        {% thumbnail work.data.thumbnail, work.data.title %}
                        <div class="content">
                            <h3>{% if work.data.client %}{{ work.data.client }}: {% endif %}{{ work.data.title }}</h3>
                            {% if work.data.description %}
                                <p>{{ work.data.description }}</p>
                            {% endif %}
                        </div>
                    </a>
                </li>
            {% endif %}
        {% endfor %}
    </ol>
</section>
{% include 'testimonials.njk' %}
<section id="bits" class="section">
    <div class="t-container">
        <h2>Latest Bits</h2>
        <p>Some of projects that I have dealt with during my career, more info is <a href="#contact" title="Go to the Contact section">available uppon request</a>.</p>
    </div>
    <ol>
        {% for work in collections.work %}
            {% if work.url == false %}
                <li class="work">
                    {% thumbnail work.data.thumbnail, work.data.title %}
                    <div class="content">
                        {% if work.data.nft %}
                            <span class="label">NFT</span>
                        {% endif %}
                        <h3>{% if work.data.client %}{{ work.data.client }}: {% endif %}{{ work.data.title }}</h3>
                        {% if work.data.description %}
                            <p>{{ work.data.description }}</p>
                        {% endif %}
                    </div>
                </li>
            {% endif %}
        {% endfor %}
    </ol>
</section>
{% include 'personal.njk' %}
{% include 'contact.njk' %}